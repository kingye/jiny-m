/**
 * Email channel configuration types.
 *
 * Re-exports from types/index.ts for convenience when working
 * with email channel code specifically.
 */

export type { ImapConfig, SmtpConfig, WatchConfig, EmailChannelConfig } from '../../types';
